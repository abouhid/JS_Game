import Entity from '../src/Entities/Entity';
import Phaser from 'phaser'

// test('Checks if Entity is a subclass of Phaser', () => {
//   expect(Entity).toBeSubclassOf(Phaser.Physics.Arcade.Sprite);
// });